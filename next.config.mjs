/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add the domain here
      },
    };


// next.config.mjs


export default nextConfig;
